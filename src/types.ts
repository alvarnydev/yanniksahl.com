export interface Degree {
  title: string;
  image: string;
  when: string;
  where: string;
  special: string;
  grade: string;
}

export interface Experience {
  role: string;
  when: string;
  where: string;
  company: string;
  tasks: string;
}
